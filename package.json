{
  "name": "format-si-unit",
  "main": "dist/index.js",
  "type": "module",
  "version": "0.0.3",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup-node ./index.ts --format esm --sourcemap --dts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "tsup": "^8.3.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
